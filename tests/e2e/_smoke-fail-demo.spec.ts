import { test, expect } from "@playwright/test";

// 🎬 Test de démo pour M8.L4 — déclencher la notif Teams sur échec @smoke
// À supprimer après le tournage de la leçon
test("@smoke démo notif Teams — fail volontaire", async () => {
  expect(true).toBe(false);
});
